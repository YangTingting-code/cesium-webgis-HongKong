// function togglePanel() {
//   sidebarOpen.value = !sidebarOpen.value
  
//   if (sidebarOpen.value) {          // 想打开
//     const ok = store.tryOpenPanel('spatialSearch')
//     if (!ok) {
//       sidebarOpen.value = false
//       ElMessage.warning('请先关闭其他功能面板或停止运行')
//     } 
//   }else{
//     store.closePanel('spatialSearch')
//   }

// }